# Copy to .env and fill in. .env is gitignored — never commit it.
# Values here override the shell-wide ANTHROPIC_API_KEY export in ~/.zshrc,
# so hood's key can be rotated independently of other bots in the fleet.
ANTHROPIC_API_KEY=
