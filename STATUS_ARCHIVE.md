# STATUS_ARCHIVE — hood_agent_1

_Full pre-2026-07-10 STATUS.md, preserved verbatim when STATUS was pruned to a lean at-a-glance file. Nothing here was deleted — only moved out of the live STATUS. Deep engineering history also lives in LEDGER.md / AUDIT_LEDGER.md._

---

# hood_agent_1 — STATUS

> Standardized header. Keep these fields at the very top, always current.
> Detailed forensic history lives in `AUDIT_LEDGER.md` + the `GROK_HANDOFF_*` / `*_CHANGELOG.md` files. This file is the at-a-glance map.

- **One-liner:** Microcap event-driven agent — EDGAR filings → EV distributions → adversarial audit → risk-capped execution. Uses EV distributions, not conviction scores; deterministic screens before any LLM.
- **Stage:** paper-validating
- **Live gate:** OFF (LIVE_ENABLED=false; human go-live gated)
- **Tests:** 120 passing (81 test_core + 26 test_audit_runner_gate [+G12 Opus temperature-retry] + 6 veto + 4 replay integrity + 3 emit gate; verified via full `unittest discover`, not just individual files).
- **Intelligence type:** Full LLM reasoning with EV distributions + two-part adversarial Auditor. Cheap-model-first: Haiku 4.5 triage (high-frequency), Opus 4.8 reserved for the rare genuine trade decision (EV thesis + adversarial audit).
- **Single most important next thing:** The OOS clock has had THREE consecutive silent-death bugs since 2026-07-01, each one only revealing the next once fixed: event_type mismatch → filing-text truncated to pure XBRL markup → Opus 4.8 rejecting a `temperature` param. All three now fixed and fail-before verified (G10/G11/G12). **Still no real-world confirmation the fixes actually produce a trade** — the first run after the temperature fix (2026-07-10 9:35 AM) came back clean but empty: 13 candidates passed the feed's free pre-filter, all 13 were duplicates already processed on a prior day (correctly deduped), so 0 new candidates / 0 LLM calls / $0 spent — not a bug, just a quiet morning, but also not yet a real test. Keep watching subsequent runs for the first day with genuinely NEW candidates; that's the actual first fair test, still pending. If candidates flow but nothing resolves, check for a fourth bug before assuming "no +EV events yet." Separately: switch deployment from cron (launchd, 9:35 AM/3:35 PM) to a real always-on process (`run_paper.py --max-cycles 0`). Then: accumulate N≥20 resolved trades across ≥2 real regimes for the Phase-4 calibration gate. Current regime: `riskon_calm`.
- **Honest odds this makes money:** Research bet, ~25–35%. Strongest audit discipline in the portfolio; weakest integration layer.
- **Security posture:** Secrets gitignored (.env/.pem/.key, 2026-06-07; not a git repo yet). Least-agency strongest in portfolio (safety veto + risk caps + SafetyCore + use-time invariant). TODO before live: injection-harden EDGAR→EV/Auditor path (`GROK_HANDOFF_SECURITY_INJECTION.md`), Robinhood read-only first. See `DEFINITION_OF_DONE.md`.
- **Last updated:** 2026-07-10 (checked the first post-temperature-fix trading day; morning run clean but zero new candidates — all 13 free-filter passes were prior-day duplicates, correctly deduped. No new bugs, but the fixes still haven't seen a real candidate to prove out yet. 120/120 tests, unchanged this session — investigation only, no code changed)

---

## Stage vocabulary
`idea → skeleton → core-done → runner-wiring → paper-validating → live-gated → live`

## Recent movement
- 2026-07-10: Asked "how is today's trading day going as the genuine first fair test" (post-G12 temperature fix). No code changes — pure investigation. 9:35 AM scheduled run completed cleanly (exit 0, no errors of the three previously-fixed types). One transient EDGAR efts 500 on the first page fetch, self-recovered (157 raw filings pulled anyway). Feed's free pre-filter passed 13 events to triage across the run, but ALL 13 turned out to be duplicates already processed on a prior day (the feed rescans "today + yesterday" every run; yesterday's genuinely-new filings correctly get skipped once seen via the persisted processed_events.json, now 411 entries). Net: candidate_events_today=0, llm_calls_today=0, $0 spent, 0 new graveyard rows. Not a bug — just a quiet morning — but also not yet a real test of the three fixes. Afternoon (3:35 PM) run hadn't fired yet as of this check.
- 2026-07-09: Asked "what is the OOS data telling us" — graveyard had genuinely grown (18→76 rows) but still 0 resolved trades. Breaking down all reject reasons found a THIRD silent-death bug: 25 of 76 rows (33%) were a real Anthropic 400 error, "`temperature` is deprecated for this model," dated 2026-07-07 through today — starting the day after the filing-text fix, i.e. the first day real candidates had substantive content to reach the Opus (EV thesis) call at all. Root cause: the 2026-06-30 Sonnet→Opus 4.8 switch passes an explicit `temperature=` to every EV/auditor call; `claude-opus-4-8` is apparently an extended-thinking model (confirmed live via `output_tokens_details: {thinking_tokens: 0}` in the real response) that hard-rejects a user-supplied temperature. Every real EV-thesis attempt for 3 days died this way — correctly fail-closed (no fabrication), but for a parameter bug, not a real decision. Fixed with a single BOUNDED, parameter-correcting retry in `AnthropicLLMClient.complete()` (only fires when the error specifically identifies temperature as incompatible; any other error fails through unchanged — not a blind retry). Locked with G12 (2 tests), fail-before genuinely verified. Verified against the REAL live Anthropic API (a direct Opus call with the fix now succeeds) and via a real 10-cycle run_paper.py pass (30 real LLM calls, $0.058 spent, zero new temperature errors — new rejections are correctly `budget_refused_no_thesis` instead). Three consecutive silent-death bugs since 2026-07-01, each masking the next until fixed; this is the first point a real candidate could plausibly clear the whole pipeline. 120/120 tests.
- 2026-07-06: Asked "how's the OOS clock doing" and found two more silent-death bugs, plus a data-integrity issue. (1) `data/decisions.ndjson` (build #6 telemetry) looked like it showed 36 real filled trades — verified it's 100% test-suite contamination (60/76 records had synthetic fixture tickers/IDs from the unit test suite; only 16 were real). Root cause: hardcoded `DEFAULT_DECISIONS_PATH` + `ReactionLayer` defaulting `emit_decisions=True` meant every test run wrote into the same file the real OOS run uses. Fixed: default flipped to `False` (opt-in), `run_paper.py` explicitly opts in with a path scoped to its own `--data-dir`; verified full test suite run leaves the file's line count unchanged. Cleaned the file down to its 16 real records (backup kept). (2) Found the identical bug class already hit `EdgarClient`'s cache_dir (defaulted to `Path("data")`, ignoring `--data-dir` — the real OOS cache was orphaned in `./data/edgar_cache.db` while `data_real/edgar_cache.db` sat stale since 2026-06-29); fixed both call sites. (3) The REAL finding: 100% of real candidates since the 2026-07-01 fix were being rejected by Haiku triage as "8-K header only" — root cause confirmed against a real cached filing: modern SEC filings are inline-XBRL HTML, and truncating the RAW HTML to 8000 chars before sending it to the LLM meant the entire budget was consumed by XML namespace/XBRL cover-page markup before any Item disclosure text appeared. Fixed with a new `strip_html_to_text()` (stdlib regex tag-stripper) applied before truncation in both event feeds; verified on the real sample (raw 51,124 chars → cleaned 19,337, "Item 1.01" now at ~char 2,300, well inside budget). Locked with G11 (3 tests), fail-before genuinely verified. Net: the clock has had two consecutive masking bugs since 2026-07-01 and effectively restarts today. 118/118 tests (verified via full `unittest discover`).
- 2026-07-05: Build #6 — `src/core/decision_emit.py` shared with replay; `ReactionLayer` + `phase2` emit `mode:paper` to `data/decisions.ndjson` (fail-safe, ~5% `screen_reject` sample). 114/114 tests.
- 2026-07-05: Build #5 replay — `replay/` emits `mode:replay` decisions (PIT market data, budget breaker, hermetic slice → `data/replay/REPLAY_REPORT.md`). 112/112 tests.
- 2026-07-05: Fable edge-recon vetoes V1–V4 in deterministic screens (`veto_params.py` + `auditor.py`); `run_auditor` skips adversarial LLM on all hard vetoes. Hood does not ingest 13Ds yet — V4 guard is defensive.
- 2026-07-01: Found + fixed a 3-day-old silent bug: DynamicEDGARFeed tagged every trigger event_type=f"8-K items={item_label}" (e.g. "8-K items=2.02,5.02"), which never matched ReactionLayer's allowed_event_types — every single candidate died at the first tier1 check since the feed went live 2026-06-29. Surfaced by the previous day's new candidate_events_today metric (19 candidates, 0 LLM calls — the mismatch was invisible before that). Fixed to match SimpleEDGARFeed's working convention; locked with G10 (fail-before verified: reproduced the exact bug, confirmed the fix). Verified against real EDGAR + real Anthropic API, not just tests: candidate_events_today 19→26, llm_calls_today 0→6, real spend $0→$0.0174 in a 15-cycle run. OOS clock is now genuinely alive for the first time. 102/102 tests.
- 2026-06-30: Cost-efficiency mandate (watcher/reasoner split + hard spend breaker + cheap-model-first + prompt caching, see `GROK_HANDOFF_COST_EFFICIENCY.md`). Locked the watcher/reasoner split (already structurally correct, now tested: G8) and the spend breaker (G9), both fail-before verified. Fixed a real bug that was silently capping hood to ~1 candidate/day regardless of actual cost (tier1's budget pre-check used a flat $0.10 estimate — the whole daily cap — instead of the real next-call cost). Escalated EV thesis + adversarial audit from Sonnet to Opus 4.8 explicitly (Haiku 4.5 stays the triage model). Both LLM clients now capture and bill Anthropic's cache token fields (previously dropped entirely — cache reads/writes were being billed at flat full price). Fixed hood_state.json's budget_remaining_usd (silently null since introduced) + added an llm_metrics block. 101/101 tests.
- 2026-06-07: Portfolio review baseline. Headline from own pre-Robinhood audit: "Do NOT wire Robinhood to a no-op runner." Library phase-audited and trustworthy; runner non-functional for real operation.
- 2026-06-07: Security pass — gitignore hardened (.env/.pem/.key); wrote GROK_HANDOFF_SECURITY_INJECTION.md (delimit/label untrusted EDGAR text into EV engine + Auditor, fail-safe schema validation); DEFINITION_OF_DONE.md added.
- 2026-06-07: CL-1 auditor re-verification — dedup/restart test genuine (✅); e2e test correctly calls run_paper (✅); two-phase confirm is cosmetic scaffolding for mock/paper (🔴 uncovered); unwind-on-confirm-fail absent (🔴). Wrote GROK_HANDOFF_CL1_REMAINING.md with numbered fix list + fail-before standard.
- 2026-06-09: CL-1 Fix 1+2 complete (auditor standing in for Grok). confirm-fails-no-open covered by FakeBrokerConfirmFail test; unwind-on-confirm-fail implemented (cancel_all + confirm_fail_unwind result); fail-before captured; 53/53.
- 2026-06-10: RH MCP connected. Surveyed live tool surface — real schemas verified. Reconciled RealRobinhoodMCPClient (tool names, response shapes, account_number). Agentic account 981398050 ($0 fresh). CRKN unlisted/no-market handled correctly. 53/53.
- 2026-06-10: CL-2 complete. Auditor-owned gate file `tests/test_audit_runner_gate.py` — 5 gates (CalibrationGate fail-closed, Section 11 veto, EV no-fab, SafetyCore fail-closed, Runner e2e) locking the hardest-won invariants. 66/66.
- 2026-06-27: Universe rebuilt empirically — 713 recent 8-K/S-3 filers screened via the bot's own EDGAR + live quotes → 38 names (30 tradeable-now small-caps + 8 microcap watch). Verified: bot's real feed yields 60+ events across 15 tickers (dead universe yielded 0). Surfaced finding: Section-11 2% spread veto blocks true microcaps on Yahoo → Tier A validates process, microcap edge needs RH read path. 69/69.
- 2026-06-27: Haiku triage gate built (cheap pre-filter before dual-Sonnet; ~5× cheaper/event; fail-before captured). RH read path parsing-verified end-to-end on real-shaped responses — proved real broker spread unblocks tight Tier-B microcaps through Section 11 (live broker still owner-gated: needs MCP connected + Claude-orchestrated). Stub/broken-path audit → 🔴 CRITICAL: `regime="live"` hardcoded means the calibration gate can never pass (needs ≥2 regimes); realized-return path confirmed honest. 72/72.
- 2026-06-27: Remediated the audit findings. (1) Regime blocker FIXED — new `src/core/regime.py` (IWM trend×vol → e.g. `riskon_stressed`, live-verified), stamped immutably at open time (no more `regime="live"`). (2) Auditor now FAILS CLOSED when it can't run (kill/budget/exception → no trade), lenient only on successful-but-unparseable output. (3) `prior_accuracy` relabeled `_name_win_rate_prior` (honest: crude self win-rate, not calibrated accuracy). 75/75.
- 2026-06-27: Gate regime floor (owner-approved). CalibrationGate now requires ≥2 real regimes EACH ≥5 resolved (configurable) and excludes `unknown` from diversity — locked in auditor gate file G1 (thin tail→FAIL, unknown-padded→FAIL, control→PASS). All known gate/data blockers cleared. 76/76.
- 2026-06-28: OOS clock started. Pipeline driven end-to-end on real AKTX June-26 8-K: regime=riskon_stressed, real Sonnet EV engine fired (EV=-9.4%, negative — correctly rejected by auditor for XBRL-only content + S-1 dilution flag). $0.047/cycle verified. Cron wired (run_oos.sh, 9:35 AM + 3:35 PM CDT weekdays). SafetyCore log_violation mkdir fix (76/76 clean). Stage → paper-validating.
- 2026-06-30: Tenant isolation + go-live groundwork, then audited a second time on owner pushback. (1) OwnershipLedger built — hood's exclusive record of positions it opened; sell gate in executor vetoes sells on tickers not in ledger (not_in_hood_ledger); fail-closed on corrupt/missing file; persisted atomically. (2) Sleeve sizing fixed — RiskController and mock now initialized from HOOD_SLEEVE_USD env var (default $500), not hardcoded $100k; current_book_usd sourced from risk.agentic_sleeve_usd, not constant 50000. (3) Two safety invariants labeled and documented in INVARIANTS.md: LIVE_GATE_HARD_RAISE + CONFIRM_AFTER_FILL_OR_UNWIND. (4) State snapshot: hood_state.json written after each cycle. (5) max_cycles=None for always-on go-live mode (--max-cycles 0 in CLI).
  **Owner caught a real gap on first report: the two invariants had no fail-before-verified gate-suite test, and the initial report didn't supply one.** Closed by adding G6 (tenant isolation, 2 tests) and G7 (live-gate hard-raise, 2 tests) to the locked `test_audit_runner_gate.py`, each with a genuine fail-before run (auditor temporarily reverted the fix, confirmed the test fails, restored, confirmed it passes). Found and fixed a real bug along the way: the first G6 rollback test used ticker "ROLLBACK" (8 chars) which silently hit an unrelated pre-existing schema rule (`EVThesis.validate()`: ticker must be ≤6 chars) — `build_ev_thesis` returned None before ever reaching the executor, making the test pass for the wrong reason regardless of the fix. Renamed to a valid ticker and reran fail-before; genuine this time. Also caught: INVARIANTS.md cited a test name (`test_live_gate_raises_if_not_armed`) that never existed in the codebase — replaced with the real G7 test. 91/91 tests, 18/18 in the locked gate file.
- 2026-06-30: launchd TCC root cause resolved. ~/Desktop is a macOS privacy-protected folder — launchd-spawned processes have no Desktop access, so every scheduled run silently exited 78 (EX_CONFIG) without writing a single log line. Fix: grant /bin/bash Full Disk Access in System Settings → Privacy & Security; move plist StandardOutPath/StandardErrorPath to /tmp (launchd opens those directly). Confirmed: kickstart now exits 0, log writes correctly, scheduled runs will fire.
- 2026-06-29: DynamicEDGARFeed built. Scans EDGAR efts API for all 8-K/S-3 filed today (~90/day) → free pre-filters (inv-co exclusion, pure-exhibit skip, CIK→ticker reverse map, Yahoo price $2–$20/ADV >$1M/spread <3%) → 2–10 candidates/day reach Haiku at ~$0.02 cost, leaving $0.08 for Sonnet. EDGAR DEFAULT_UA fixed (real email; fake was causing SEC throttling). launchd exit-78 fixed (source || true). `run_oos.sh` updated to --feed dynamic.
- 2026-06-17: First real OOS run attempt. Fixed EDGAR gzip + company_tickers dict-shape bugs (was crashing all tickers). Added 90-day staleness filter (skip old filings, no LLM spend) + 8k text cap. Owner cost controls: $0.10/day down-only cap (via SafetyCore audited path) + `--market-days-only` (stdlib NYSE calendar; $0 on closed days). Found + recorded: NO Haiku triage gate exists in live path (every event = Sonnet×2) — top remaining cost fix. Universe still dead (must replace). 69/69.
